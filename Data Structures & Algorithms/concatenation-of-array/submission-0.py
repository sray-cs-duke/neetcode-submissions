class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        new = []
        for num in nums:
            new.append(num)
        for num in nums:
            new.append(num)
        return new
